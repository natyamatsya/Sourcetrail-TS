// Member definitions are now inline in TokenComponentBundledEdges.inl (included by the header); this TU
// just compiles the header in the classic (non-module) build.
#include "TokenComponentBundledEdges.h"
