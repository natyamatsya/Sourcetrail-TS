// SearchMatch's member definitions now live inline in SearchMatch.inl (included by SearchMatch.h) so
// they resolve for module importers. This TU only pulls in the header to keep the build target stable.
#include "SearchMatch.h"
