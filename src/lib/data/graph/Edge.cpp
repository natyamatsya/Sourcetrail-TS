// All member definitions are inline in Edge.inl (pulled in -- once both Edge and Node are complete --
// via the Node.h include chain) so they resolve for both the classic and the C++20-module build. This
// TU only anchors the header.
#include "Edge.h"
