// All member definitions are inline in NodeType.inl (pulled in by the header) so they resolve for both
// the classic and the C++20-module build. This TU only anchors the header.
#include "NodeType.h"
