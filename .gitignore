# CMake
CMakeUserPresets.json

# Maven, Vcpkg:
/java_indexer/target/
vcpkg_installed/

# IntelliJ, Eclipse, Qt Creator:
.vs
.vscode
CMakeLists.txt.user

# Sourcetrail databases, bookmarks
*.srctrl.bm
*.srctrl.db
*.srctrl.db-*
*.srctrl.db.tmp
*.srctrl.db.tmp-*
# Legacy artifacts (no runtime compatibility, ignore only for local cleanliness)
*.srctrl.db_tmp
*.srctrl.db_tmp-*
*.srctrlbm
*.srctrldb
*.srctrldb-*
*.srctrldb_tmp
*.srctrldb_tmp-*
/.build/

# Rust / Cargo
src/rust_indexer/target/
# Cargo.lock IS tracked: rust_indexer and agent_mcp_bridge are binaries, so we
# commit the lock for reproducible builds (matches src/turso_shim). See the
# note in src/agent_mcp_bridge/.gitignore.

# Swift Package Manager (Swift indexer)
src/swift_indexer/Package.resolved
src/swift_indexer/Sources/SourcetrailSwiftIndexerCore/GeneratedFromCMake

# Sourcetrail bookmark-DB WAL sidecars (mirror of the *.srctrl.db-* rule)
*.srctrl.bm-*

# Turso-shim comparison-mode DBs (generated on project load when
# SOURCETRAIL_TURSO_COMPARE is on; not source)
*.turso
*.turso-wal

# Temporary reference extraction
/inspiration/download/
/inspiration/download.zip
